import React from "react";

const Header = () => {
  return (
    <div className="header">
      <h3 className="logo">Agenda App</h3>
      <button className="login-btn">Log in</button>
    </div>
  );
};

export default Header;
